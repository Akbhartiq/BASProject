<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding-left: 0;
            background-color: #f5f5f5;
            background-image: url("https://images.unsplash.com/photo-1513001900722-370f803f498d?q=80&w=1887&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D");
            background-size: 40%;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .book-card {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            overflow: hidden;
            width: calc(50% - 20px);
            /* Two books in one row */
            margin-bottom: 20px;
            margin-left: 10px;
            margin-right: 10px;
        }

        .book-details {
            padding: 20px;
            text-align: center;
        }

        .book-image {
            max-width: 200px;
            height: 200px;
            border-radius: 20px;
            display: block;
            margin: 0 auto;
            /* Center the image */
            margin-top: 10px;
        }

        .book-title-container {
            background-color: #a42674;
            margin-left: 10%;
            margin-right: 10%;
            border-radius: 10cm;
        }

        .book-title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
        }

        .book-info {
            margin-bottom: 10px;
            color: #666;
            font-size: 16px;
        }

        .book-isbn {
            margin-bottom: 10px;
            color: #666;
            font-size: 16px;
        }

        .book-price {
            font-size: 20px;
            font-weight: bold;
            color: #28a745;
        }

        .book-count-wrapper {
            margin-top: 25px;
            background-color: rgb(0, 255, 204);
            border-radius: 20px;
            margin-left: 500px;
            margin-right: 500px;
            cursor: pointer;
            /* Add cursor pointer to indicate clickability */
        }



        .submit-button {
            background-color: #007bff;
            /* Button background color */
            color: #fff;
            /* Text color */
            border: none;
            /* Remove border */
            border-radius: 5px;
            /* Add rounded corners */
            padding: 10px 20px;
            /* Add padding */
            font-size: 16px;
            /* Font size */
            cursor: pointer;
            /* Add cursor pointer */
            transition: background-color 0.3s ease;
            /* Smooth transition for hover effect */
            display: block;
            margin: 20px auto;
            width: 150px;
        }

        .submit-button:hover {
            background-color: #0056b3;
            /* Change background color on hover */
        }

        .book-count {
            text-align: center;
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="book-count-wrapper"> <!-- Add onclick event -->
        <div class="book-count">
            <%= books.length %> Books Found
        </div>
    </div>
    <div class="container">
        <% books.forEach((book, index)=> { %>
            <div class="book-card">
                <% let image_link=book.get("image_url_l") %>
                    <img src="<%= image_link %>" alt="Book Cover" class="book-image">
                    <div class="book-details">
                        <div class="book-title-container">
                            <h2 class="book-title">
                                <%= book.get("title") %>
                            </h2>
                        </div>
                        <p class="book-info"><b>Author:</b>
                            <%= book.get("author") %>
                        </p>
                        <p class="book-info"><b>Publication Date:</b>
                            <%= book.get("publication_date") %>
                        </p>
                        <p class="book-isbn"><b>ISBN:</b>
                            <%= book.get("ISBN") %>
                        </p>
                        <p class="book-info"><b>Genre:</b>
                            <%= book.get("genre") %>
                        </p>
                        <p class="book-info"><b>Copies Available:</b>
                            <%= book.get("frequency") %>
                        </p>
                        <p class="book-price">₹<%= book.get("price") %>/-</p>
                        <form action="/get_reciept" method="POST">
                            <!-- Hidden input field to pass information -->
                            <input type="hidden" name="title" value=<%=book.get("title") %>>
                            <input type="hidden" name="author" value=<%=book.get("author") %>>
                            <input type="hidden" name="price" value=<%=book.get("price") %>>
                            <input type="hidden" name="isbn" value=<%=book.get("ISBN") %>>
                            <!-- Other input fields or form elements can be included here -->
                            <button type="submit" class="submit-button">Submit</button>
                        </form>
                    </div>
            </div>
            <% if ((index + 1) % 2===0) { %>
                <div style="width: 100%;"></div> <!-- Add a clear fix after every second book -->
                <% } %>
                    <% }) %>
    </div>
</body>

</html>